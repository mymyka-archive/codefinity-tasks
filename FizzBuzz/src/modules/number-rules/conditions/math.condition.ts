export const isDivisibleBy = (devider: number) => (arg: number) =>
  arg % devider === 0;

// Possible extensions
// export const isEven = (arg: number) => arg % 2 === 0;
// export const isOdd = (arg: number) => !isEven(arg);
